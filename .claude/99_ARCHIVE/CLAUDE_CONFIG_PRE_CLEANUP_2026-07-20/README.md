# Claude configuration rollback — 2026-07-20

This folder preserves the pre-hardening `settings.local.json` and all five live workspace skill files before the 2026-07-20 configuration cleanup.

The archived settings contained legacy absolute paths and broad mutation/install/deletion allowances. It is rollback evidence, not an approved configuration. Restore only after reviewing every permission against the current workspace and security rules.

No credentials were found in the reviewed JSON or skill files.
